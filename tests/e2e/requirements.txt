# Dependencias Python para la suite E2E (tests/e2e)
# Instalar con: pip install -r requirements.txt
# Y descargar el navegador: python -m playwright install chromium --with-deps
pytest>=8.0
# >=1.50 para wheels en Python 3.13/3.14 (greenlet); CI usa 3.12 y también funciona.
playwright>=1.50.0
