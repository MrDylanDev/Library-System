# === PERFIL DE DESARROLLO ===
# Activar: --spring.profiles.active=dev
# Nota: application.properties ya tiene valores sensatos para dev.
# Este archivo existe para sobreescribir propiedades específicas si es necesario.

# Ejemplo: apuntar a PostgreSQL local en lugar de H2
# spring.datasource.url=jdbc:postgresql://localhost:5432/libromagico
# spring.datasource.username=dev
# spring.datasource.password=dev
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
