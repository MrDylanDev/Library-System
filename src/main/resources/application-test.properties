app.mail.enabled=false

# Rate limiting desactivado en tests para no contaminar la suite existente
auth.rate-limit.enabled=false

# Actuator: sin indicador de mail en tests (no hay SMTP; el health global
# debe reflejar solo la BD, no un SMTP inexistente)
management.health.mail.enabled=false
